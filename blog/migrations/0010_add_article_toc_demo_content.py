from django.db import migrations


ARTICLE_BODIES = {
    'Vue 3 组合式 API 实战技巧': '''Vue 3 引入的组合式 API（Composition API）为组件逻辑的组织和复用提供了全新方式。

## 为什么需要组合式 API
相比 Options API，Composition API 允许我们按功能而非选项类型来组织代码，使得相关逻辑更加内聚。

当组件变得复杂时，数据、方法、监听器和生命周期常常分散在不同选项里。组合式 API 可以把同一业务能力放在一起，阅读和维护都会更顺。

### 从选项到功能
过去我们常按 data、methods、computed 拆代码。现在可以按搜索、分页、权限、表单校验等功能拆分代码。

## 响应式数据与派生状态
关键特性包括 ref 和 reactive 创建响应式数据、computed 计算属性、watch 和 watchEffect 侦听数据变化。

ref 适合基本类型和明确的单值状态，reactive 更适合组织对象状态。computed 用来表达可缓存的派生数据，watch 则适合处理副作用。

### watch 与 watchEffect
watch 更强调“监听谁”，watchEffect 更强调“依赖什么就自动追踪什么”。在业务代码里，明确依赖关系通常更容易长期维护。

## Composables 的复用方式
通过自定义组合函数（Composables），我们可以将组件逻辑提取为可复用的函数，在不同组件之间共享状态和行为。

一个好的 Composable 应该命名清楚、返回值稳定，并尽量少依赖组件内部上下文。这样它既能被页面复用，也方便单独测试。

### 实战建议
把网络请求、表单状态、浏览器事件、权限判断拆成小的组合函数。每个函数只解决一个问题，组合起来就是完整页面逻辑。''',

    'Django 入门：从零搭建个人博客': '''Django 是 Python 最流行的 Web 框架之一，以其“开箱即用”的设计理念著称。

## 项目初始化
本文将带你从零开始，一步步搭建一个具有文章管理、分类标签、评论功能的个人博客。

第一步是创建项目和应用，配置数据库、模板目录与静态文件路径。完成这些基础工作后，项目结构会变得清晰很多。

### 应用边界
博客应用负责文章、分类、标签和评论；项目配置负责全局路由、数据库和部署参数。边界清楚，后续扩展会轻松许多。

## 理解 MTV 架构
首先，我们需要了解 Django 的 MTV（Model-Template-View）架构模式：

Model 定义数据结构和数据库操作，Template 负责页面展示，View 处理业务逻辑并连接 Model 和 Template。

### 模型设计
文章通常需要标题、正文、摘要、封面、分类、标签、作者、发布时间和阅读量。评论则关联文章并保存昵称、邮箱和正文。

## 后台与路由
Django 的 ORM 让数据库操作变得简单直观，你只需定义 Python 类，Django 就会自动为你创建数据库表。

通过内置的 Admin 后台，你可以方便地管理数据内容。路由系统使用 URLconf 配置，将 URL 映射到视图函数或类视图。

### 模板复用
模板引擎支持继承和组件化。把导航、页脚、侧边栏放进基础模板，具体页面只关注自己的正文内容。''',

    'Docker 容器化部署最佳实践': '''Docker 已经成为现代应用部署的标准工具。本文分享经过实战验证的 Docker 最佳实践。

## 镜像构建
使用多阶段构建可以显著减小镜像体积。构建阶段安装依赖、编译资源，运行阶段只保留真正需要的产物。

合理利用构建缓存也很重要。先复制依赖清单再安装依赖，最后复制业务代码，可以避免每次改代码都重新安装全部依赖。

### .dockerignore
把日志、缓存、虚拟环境、测试产物和本地数据库排除掉，可以让构建上下文更小，镜像构建也更快。

## 运行时安全
生产环境里不要以 root 用户运行容器。为应用创建专用用户，并只授予必要权限，可以降低意外操作的影响范围。

健康检查同样重要。它能让编排系统及时发现不可用实例，并自动重启或摘除异常容器。

### 环境变量
数据库地址、密钥、第三方服务凭据都应该通过环境变量注入，不要写进镜像或代码仓库。

## 编排与发布
在生产环境中，建议使用 docker-compose 或 Kubernetes 来编排多个容器服务，同时配合 CI/CD 流水线实现自动化部署。

发布流程最好包括测试、构建、推送镜像、部署和回滚策略。这样每次上线都有明确路径。''',

    '大语言模型应用开发实战': '''大语言模型（LLM）正在改变软件开发的方式。

## Prompt Engineering
提示词工程的核心是把任务、角色、上下文和输出格式讲清楚。好的提示词不是越长越好，而是约束明确、信息足够。

角色设定可以让模型进入合适的回答视角，少样本示例能稳定输出结构，清晰的格式要求则方便后续程序解析。

### 常见模式
可以把提示词拆成系统目标、输入资料、处理步骤和输出格式四个部分。复杂任务尽量分阶段完成。

## RAG 检索增强生成
RAG 的目标是让模型基于你的知识库回答问题。流程通常包括文档切分、向量化、语义检索和增强生成。

文档切分太粗会丢细节，太细会丢上下文。实践中需要根据内容类型不断调参。

### 检索质量
评估 RAG 不能只看模型回答，还要看检索片段是否准确。检索错了，生成再流畅也可能偏题。

## Agent 与工具调用
Agent 开发让 LLM 可以使用外部工具，自动分解任务并执行多步操作。

工具调用适合查数据库、访问 API、运行计算、生成文件等确定性任务。模型负责决策，工具负责执行。

### 落地建议
推荐从小工具开始，不要一上来就做完全自主的复杂 Agent。先把边界收紧，稳定后再逐步扩展。''',
}


def add_toc_demo_content(apps, schema_editor):
    Post = apps.get_model('blog', 'Post')
    for title, body in ARTICLE_BODIES.items():
        Post.objects.filter(title=title).update(body=body)


class Migration(migrations.Migration):

    dependencies = [
        ('blog', '0009_seed_mainland_friend_links'),
    ]

    operations = [
        migrations.RunPython(add_toc_demo_content, migrations.RunPython.noop),
    ]
